# Dashboard Reference Artifacts

This folder stores version-controlled dashboard artifact paths referenced by
`dashboards/specs/core_trio.json`.

The checked-in PNG files are exported reference captures from the current live
Metabase layouts. Keep these artifacts aligned with the latest published Core
Trio dashboard state so repository validation and human-readable documentation
point to the same layout.
