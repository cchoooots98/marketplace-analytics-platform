# Dashboard Reference Artifacts

This folder stores version-controlled dashboard artifact paths referenced by
`dashboards/specs/core_trio.json`.

The checked-in SVG files are lightweight layout captures so the repository can
validate expected dashboard artifact paths before a live Metabase instance is
connected. Replace them with exported Metabase screenshots after the local
runtime is configured and the Core Trio dashboards are populated.
